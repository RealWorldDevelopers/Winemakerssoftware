@inject Microsoft.AspNetCore.Identity.SignInManager<ApplicationUser> SignInManager
@inject Microsoft.AspNetCore.Identity.UserManager<ApplicationUser> UserManager
<div class="text-right">
   @if (SignInManager.IsSignedIn(User))
   {
      <form asp-area="" asp-controller="Account" asp-action="LogOut" method="post" id="logoutform">
         <button type="submit" class="btn btn-link navbar-btn navbar-link">Log out</button>
      </form>
   }
   else
   {
      <a class="nav-link" asp-area="" asp-controller="Account" asp-action="Login">Log In</a>
   }
</div>
<div class="card">
   <div class="card-header bg-secondary text-light">
      <h1>What's New</h1>
   </div>
   <div class="card-body">
      <h2>Journal Notes</h2>
      <p><a asp-area="" asp-controller="Account" asp-action="Login">Registered Users</a> can now keep batch notes online?  Use our <a href="/journal">Journal</a>.</p>

      <h3>Available to All</h3>
      <ul class="list-group-flush">
         <li class="list-group-item">Information to help pair the right yeast.  See our <a href="/yeastpicker">Pick the Right Yeast</a> section.</li>
         <li class="list-group-item">Find a new recipe or share one.  See our <a href="/recipes">Recipe</a> section.</li>
         <li class="list-group-item">Convert Metric to Imperial (English)?  Check our the <a href="/conversions">Conversions</a> section.</li>
      </ul>

      <h3>Get Full Access for FREE</h3>
      <p>
         In an effort to keep this sight free of the growing number of bots and the increase in spam, we now limit some of this website's features to only those who are registered users.
         Get full access to all features of this site, it's <strong>SAFE</strong> and it's <strong>FREE</strong>.
         <a class="nav-link" asp-area="" asp-controller="Account" asp-action="Login">Register Now</a>

      </p>

   </div>
   <div class="card-footer bg-secondary text-light"></div>
</div>
